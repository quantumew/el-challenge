export interface FoodTruckRecord {
  Longitude: number;
  Latitude: number;
  distance: number;
  humanReadableDistance: string;
}

export interface Position {
  latitude: number;
  longitude: number;
}
